[build]
# Keep intermediates under CARGO_HOME, but isolate them by manifest path so
# concurrent worktrees cannot reuse stale local-crate artifacts.
build-dir = "{cargo-cache-home}/herdr-mcp-build/{workspace-path-hash}"
