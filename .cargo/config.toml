[build]
# Cargo 1.97 stores intermediate artifacts here while keeping final binaries,
# package output, docs, and timing reports in each worktree's own target dir.
build-dir = "{cargo-cache-home}/herdr-mcp-build"
