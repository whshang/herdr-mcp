# v0.4.6 release plan — pre-1.0 stabilization

Status: active plan. Remove or archive under `docs/history/` once v0.4.6 is published and its qualification record is complete.

## Positioning

v0.4.6 is the **last stable release before 1.0**. There is no planned `0.5.0`: after v0.4.6 the version line moves directly to 1.0. Its scope is deliberately wider than the original auth/onboarding patch that opened [PR #304](https://github.com/whshang/herdr-mcp/pull/304).

The release goal is that the ordinary **one conversation + one project** workflow is already dependable before 1.0: a new user can install and configure Herdr without avoidable setup friction; authentication and authorization boundaries are explicit and revocable; Connector, device and credential lifecycle state is inspectable; and the resident runtime remains healthy and efficient through restarts and long-lived use. 1.0 feature work (fleet control kernel, WebChat dispatch, browser control) then starts from this stable base.

Consequences of that positioning:

- The v1.0 fleet control kernel merged into `main` as [PR #305](https://github.com/whshang/herdr-mcp/pull/305) is **out of scope**. It is 1.0 feature work; v0.4.6 stabilizes what precedes it. The tag is therefore cut from `release/v0.4.6-auth-hotfix-20260904`, whose base `e9281b4` predates #305, and the release branch merges back into `main` after publication.
- Correctness and resource defects found while preparing the release are in scope even when they were not part of the original patch, provided each one lands as an independently revertable commit with its own regression test.
- Feature requests remain out of scope. [Issue #57](https://github.com/whshang/herdr-mcp/issues/57) (browser steer bridge) is 1.0 line work.

## Scope by class

### 1. Authentication and connector authority

Connector approval, request/PKCE/redirect/resource binding, deployment-secret-protected six-digit verifiers, grant revoke with durable tombstones and legacy fences, device-bound Automation Clients, unified fleet principals.

Lands as: `98ca437`, `2e813fc`, `4267e2e`, `eeb4f51`, `8701016`, `c0b72ed` on the release branch.
References: [PR #303](https://github.com/whshang/herdr-mcp/pull/303), [PR #304](https://github.com/whshang/herdr-mcp/pull/304).

### 2. First-owner onboarding and readiness

Semantic enrollment error from `worker pair` on a fresh machine, one-command first-Worker bootstrap with a release-built Edge artifact and direct Cloudflare API deployment, fresh-install Link generation reconcile, `operational_ready` separated from maintainer-only cutover sealing, actionable managed-root hint, and converged install guidance.

Lands as: `b2aafdd`, `0902414`, `713b068`, `3e890ed`.
Closes: [#306](https://github.com/whshang/herdr-mcp/issues/306), [#307](https://github.com/whshang/herdr-mcp/issues/307). [#302](https://github.com/whshang/herdr-mcp/issues/302) is implemented by the `feat/v046-first-worker-bootstrap-20260905` lane and remains release-gated on the fresh-environment UAT.

### 3. Connector, device and credential lifecycle management

The pre-1.0 stable line must have an owner-visible management path for everything that can continue to access a Worker:

- Connector inventory includes current `conn_*` instances and legacy OAuth clients that predate Connector-instance records, without exposing secrets.
- Current Connector instances can be revoked independently; legacy client grants have an explicit operator revoke path that invalidates their issued access/refresh credentials.
- Enrolled devices have a CLI inventory path in addition to rename/revoke, using immutable `device_id` for destructive actions.
- Automation credentials remain device-bound and support list/rotate/revoke. Lists expose lifecycle metadata only; plaintext secrets are returned only at create/rotate time and are never recoverable later.
- Token management is grant/principal based: operators can see non-secret token/grant counts and revoke the owning Connector/client/automation principal. The product does not add a raw bearer-token browser or persist plaintext tokens for convenience.

No new public MCP tool is required for this management surface; it stays on the enrolled-device/operator control plane.

### 4. Runtime CPU and resident behavior

[#308](https://github.com/whshang/herdr-mcp/issues/308) — a Herdr server restart leaves both EventCache streams at EOF, and because EOF is indistinguishable from a poll timeout the loop spins on closed sockets at ~100% CPU while `doctor` still reports `mode=healthy`.

Lane: `fix/v046-eventcache-eof-spin-20260904` (based on `origin/main`; `events.rs`/`state_cache.rs` do not overlap #305, so it cherry-picks cleanly onto the release branch).

Follow-on, same subsystem, deliberately **serialized after** #308 rather than parallel: the always-on polling cost — two 250 ms `poll_event` calls per loop iteration, an unconditional full `session.snapshot` every 30 s regardless of activity or client presence, and a per-SSE-client 250 ms `digest_since` that deep-clones all agents and all workspaces with no unchanged fast path. Serialized because #308 establishes the EOF/backoff invariant that idle backoff must not weaken.

Hard constraint for the polling work: **idle backoff must not change the freshness semantics of `herdr_inspect` / `herdr_since` results while a client is active.** Reduced polling must not become stale data.

### 5. Planner-facing tool integrity

- [#309](https://github.com/whshang/herdr-mcp/issues/309) — `herdr_fs_grep` returns a successful empty result for patterns that exist on disk.
- [#310](https://github.com/whshang/herdr-mcp/issues/310) — an unknown param on a mutating `herdr_call` method only warns, and the mutation then lands in the focused workspace.
- [#311](https://github.com/whshang/herdr-mcp/issues/311) — disposable named validation instances stay resident with no inventory and no reap path, and orphan plists accumulate.

Lane: `fix/v046-planner-tool-integrity-20260904` (based on the release branch tip; no cherry-pick needed).

#309 and #310 are both silent-wrongness defects: a successful response carrying a wrong result. They are in scope for a stabilization release precisely because no error surfaces and every downstream consumer acts on the false answer.

Agent supervision is also part of this stabilization surface. If a working Agent emits the same/near-identical block repeatedly with no new command result, Git diff, or task evidence, the planner must confirm from a bounded recent output tail, interrupt immediately, verify Git/task state, and restart unfinished work in a fresh Agent session from the current verified state. Already-integrated work must not be replayed. A second loop is reassigned or taken back by the main planner. This is a planner-discipline fix and does not add a public tool or autonomous resident monitor.

### 6. Transient network and backpressure failures

`429`, `502`, `503`, `504`, `524` and equivalent transient gateway failures are part of the v0.4.6 stability surface. The invariant is delivery-aware recovery: honor `Retry-After`/bounded backoff, allow bounded automatic retries only for known reads, and never replay a mutation from HTTP status alone. A structured `delivery_state=not_delivered` is sufficient replay evidence; `delivery_unknown`, an absent delivery state, or a raw gateway timeout requires live-state verification before any mutation retry. Non-JSON Cloudflare gateway responses must retain their HTTP status instead of collapsing to `invalid_edge_response`.

The browser extension already treats ChatGPT HTTP 429 as backoff-only (`30s -> 60s -> 120s`) and never uses it as a reload/resubmit trigger. Link WebSocket handshake failures already enter the bounded reconnect/route ladder, and local MCP transport already classifies 429/all 5xx as retryable transport failures. v0.4.6 closes the remaining Edge non-JSON gateway classification gap, adds explicit 524 runtime-catalog retry classification, and marks Edge capacity 429 as `not_delivered` with explicit backpressure.

Planner-tool diagnostics are also tightened without weakening mutation safety. `herdr_fs_patch` keeps rejecting ambiguous context instead of guessing a target, but `PATCH_CONTEXT_AMBIGUOUS` now returns a bounded list of 1-based candidate start lines plus truncation evidence and an actionable retry hint. This removes an avoidable read/guess cycle while preserving fail-closed patch application.

## Lane topology

| Lane | Branch | Owns | Must not touch |
| --- | --- | --- | --- |
| Release | `release/v0.4.6-auth-hotfix-20260904` | auth, onboarding, install docs, doc-assertion tests | `events.rs`, `state_cache.rs`, tool-integrity files |
| EOF spin | `fix/v046-eventcache-eof-spin-20260904` | `events.rs`, `state_cache.rs`, health reporting | edge, oauth, cli, release notes, versions |
| Tool integrity | `fix/v046-planner-tool-integrity-20260904` | fs grep, `herdr_call` validation, instance lifecycle | `events.rs`, `state_cache.rs`, install docs, doc-assertion tests |

Release notes, `CHANGELOG.md` and version numbers are owned by the integrator only, so parallel lanes cannot conflict on them.

## UAT groups

Static audit does not satisfy any of these. Each group needs a real built runtime and, where noted, a real Edge deployment.

### A. Authentication (from the independent release-candidate audit)

1. Enrolled device: `herdr-mcp connector list` lists `conn_*`.
2. New Connector completes DCR and PKCE through the browser and stops at the approval page with no code issued; only after `connector approve <request-id>` with the interactively entered six-digit code does `/oauth/token` return a JWT.
3. `connector revoke <conn_A> --confirm` invalidates A immediately while `conn_B` of the same client still works.
4. Operator revoke of a pre-v0.4.6 `client_id` with no stored record invalidates the old JWT immediately.
5. Automation: `create --name … --device <dev_*>`; omitting the device routes to the bound machine; a different device selector or ref is not delivered.
6. The copyable approval command never carries the code; a code passed on argv must fail.

### B. Onboarding

7. A genuinely unenrolled machine running `worker pair` gets a semantic enrollment error, not `Io(NotFound)`.
8. A fresh install's first remote tool call does not fail with `runtime_generation_superseded_before_dispatch`.
9. `link status` reports `operational_ready` true without sealing, and cutover gates are categorized as maintainer-only.
10. An older browser extension build shows no regression under the new inventory rule.

### C. Lifecycle management

11. `connector list` shows both current Connector instances and any legacy OAuth client registrations in non-secret form; a legacy client can be revoked explicitly and its old credentials stop working.
12. `device list` / the compatible Worker alias shows enrolled device IDs and status; rename/revoke continue to use the immutable device identity and never revoke by display name.
13. Automation list/rotate/revoke remains device-bound; rotate invalidates the previous secret immediately and list never returns plaintext credentials.
14. Management output includes only non-secret token/grant lifecycle metadata or counts. No access token, refresh token, bearer, client secret or verification code is emitted by inventory commands.

### D. Runtime CPU

15. Restart the Herdr server while the herdr-mcp candidate keeps running; sample CPU for at least 60 s and confirm no sustained spin.
16. Confirm both topology and status subscriptions re-establish automatically after the server returns.
17. Confirm `doctor` does not report `mode=healthy` while streams are at EOF.
18. Idle power comparison against v0.4.5 once the polling work lands.

### E. Tool integrity

19. `herdr_fs_grep` returns the known matches for a fixture symbol; an unscannable root errors instead of returning empty.
20. A mutating `herdr_call` with a misspelled target param is rejected before the socket call and creates nothing.
21. Instance inventory lists default and named instances; reaping a named validation instance removes its job, plist and config root without touching the default install or `~/.config/herdr-mcp/releases/**`.

## Open decisions

- **Off-`main` tag.** v0.4.5 was tagged on a `main` merge commit; v0.4.6 will be tagged on the release branch. The reason should be recorded in the release notes so `git log main` not showing the tag does not look like an error.
- **#302 qualification.** The one-command first-Worker bootstrap is implemented; keep the item open only until the exact release candidate passes fresh-environment Cloudflare deployment, canonical enrollment, Link readiness, and first remote-tool UAT.

## Qualification record

To be completed against the exact tagged source before publication, following the v0.4.5 pattern: each UAT group with its command evidence and outcome, then `scripts/validate-release-notes.mjs` as the publish gate.

### 2026-09-04 auth/onboarding candidate evidence

Candidate source: `dd0e741f75a4a864c9414205e157a76e905f876e`. This remains on the maintenance line rooted at `e9281b488e093f522020db2a2c6100d92b69499f`; PR #305 / the v1.0 fleet-control-kernel commits are not ancestors of this candidate.

- **Edge/contract regression — PASS.** `npm run test:edge` completed with `379 passed; 0 failed`, including a Notion-shaped URL Client ID Metadata Document (`https://app.notion.com/oauth/mcp-client-metadata.json`) using a public client (`token_endpoint_auth_method=none`), PKCE and a form-encoded authorization-code token exchange.
- **Focused Rust CLI regression — PASS.** `cargo test -p herdr-mcp --bin herdr-mcp cli::tests` completed `13 passed`; `worker::tests` completed `18 passed`; `cargo clippy -p herdr-mcp --bin herdr-mcp -- -D warnings` completed successfully. The six-digit verification code remains forbidden on argv but is now visible while typed in an interactive terminal because it is a short-lived verification code, not a password.
- **Local runtime activation — PASS.** `herdr-mcp dev sync` from an independent terminal activated generation `rust-28e2c5d0a006c817`, source `dd0e741f75a4a864c9414205e157a76e905f876e`, `source_dirty=false`, with `server_link_generation_reconciled=true`. A subsequent remote `herdr_inspect` reported the same source commit and `0.4.6-dev`.
- **Production Edge deployment — PASS.** `wrangler 4.129.0 deploy --config wrangler.prod.toml` uploaded `herdr-edge-prod` and returned Cloudflare Version ID `66636ec9-efb6-47dc-b3c4-a060132bf858`. Dry-run and deploy both retained the three Durable Object bindings, `herdr-edge-prod-artifacts` R2 binding, `routes=[]`, and the existing `https://herdr-mcp.agentforme.cc.cd` OAuth issuer.
- **Stable-domain approval UI / Notion CIMD preflight — PASS.** A live authorization request using Notion's real CIMD client ID and callback returned HTTP 200 from `https://herdr-mcp.agentforme.cc.cd/oauth/authorize`, title `Approve Notion`, the styled approval card, copy button, visible-code instruction, polling script, and CSP `frame-ancestors 'none'`. `/health` remained healthy with public contract epoch 3 and runtime contract epoch 2.
- **Observed pre-deploy Notion failure — reproduced as deployment drift.** Before the production Edge deploy, Notion approval succeeded in the 0.4.6 CLI but the browser returned to Notion with `Received HTTP 400`; the live approval page was still the old minimal HTML and stale authorization copy. This proves the failing transaction exercised an older Edge script rather than the release candidate tested above.
- **Post-deploy real Notion reconnect/token exchange — PENDING HUMAN BROWSER STEP.** Start a fresh Notion Custom MCP connection so it creates a new `state`/PKCE/request ID, approve it from the enrolled device, and require the browser to show `Approved. Returning to the Connector…`, return to Notion without HTTP 400, and show the connection as usable. Do not count the unit/regression test or the live preflight as a substitute for this boundary.
- **Connector inventory after the failed old-Edge attempt — clean.** `herdr-mcp connector list` returned `{"connectors":[],"ok":true}`, so there is no stale `conn_*` instance to revoke before the fresh Notion retry.

PR #304 was automatically closed when the temporary maintenance base branch was deleted. The base was restored at exactly `e9281b488e093f522020db2a2c6100d92b69499f` and #304 reopened without merge/rebase from `main`; current head `dd0e741f75a4a864c9414205e157a76e905f876e` is `MERGEABLE` and its fresh `rust` / `test` checks were running at the time of this record.

### 2026-09-06 final RC qualification evidence (pre-merge)

Final source candidate: `958c0a79a3a615b730b0294e70792c79185399e0`. The earlier `b10ecff` qualification record was not the final behavior boundary: subsequent real network/Connector UAT exposed two release-blocking reliability gaps, and the fixes plus their compatibility regression tests advanced PR #319 through `5ea545b`, `19017df`, `b25716d`, and `958c0a7`. These are therefore part of the qualified v0.4.6 candidate rather than documentation-only drift.

- **`5ea545b` — bootstrap proxy UAT fix.** Fresh first-Worker network UAT showed that the persistent Link could already use the configured local proxy while bootstrap `/health`, enrollment/OAuth verification, and final inventory still used a direct-only `reqwest` client. The fix adds one `EdgeHttpClient`: probe direct first, fall back to the already-configured proxy only after a transport failure, then reuse the selected client for the remaining bootstrap reads/mutations. `worker_bootstrap::tests::only_transport_health_failure_may_fall_back_to_proxy` proves protocol/ownership/contract validation failures remain fail-closed and cannot be converted into proxy fallback.
- **`19017df` — tool deadline vs transport-settlement deadline.** A real Claude Connector call with `timeout_ms=60000` was cut by the outer Edge request deadline at about 30 seconds even though the workstation operation later completed. The fix leaves the public/local tool timeout unchanged and gives only the Edge transport envelope a bounded settlement margin (`requested tool timeout + 5s`, capped at 65s). This does **not** relax mutation replay: unknown tools still classify conservatively as mutating; timeout for mutation/unknown stays `retryable=false`; non-JSON transient Edge HTTP automatically retries only reads; and generation retry remains allowed only when the runtime explicitly reports `delivery_state=not_delivered`. The final Edge suite covers `sent mutating on drop -> delivery_uncertain / NOT retryable`, `timeout read retryable, mutating not`, `mutating tools/call never replays a non-JSON transient Edge 502`, and `mutating call retries generation supersede only after the runtime proves it was not delivered`.
- **`b25716d` — settlement-grace regression expectation.** This is test-only: it updates the pending/limit expectation for the new Edge transport budget; no product behavior changes in this commit.
- **`958c0a7` — current-Link compatibility correction.** The first timeout repair briefly explored widening the Relay/Link wire timeout to 65s. Qualification lock correctly blocked changing the active runtime generation, and review showed the wire widening was unnecessary. The final design keeps Edge settlement capacity at 65s while capping the actual Link `timeout_ms` at the existing 60s public/local tool limit. `Edge settlement grace does not widen the legacy Link wire timeout` proves that split directly. The Rust/TypeScript Relay validators and Link daemon remain at 60s.
- **15s reconnect grace remains intact.** `b075069` defines `DEFAULT_LINK_RECONNECT_GRACE_MS = 15_000`, and the final candidate retains exactly that value. `waitForActiveLink` still uses `min(linkReconnectGraceMs, deadlineMs - now)`, so a longer Edge settlement budget cannot enlarge reconnect grace. The final Edge run also passes the reconnect-grace tests for never-connected fail-fast, bounded offline recovery metadata, process-local recent-disconnect wait/wake, and revoke waking a grace waiter fail-closed.
- **`f813723` branch duplication is historical UAT topology, not an unresolved candidate.** `origin/tmp/v046-rc-uat-20260906` is exactly `f813723` and has no unique commit. `origin/hotfix/v046-runtime-timeout-20260906` contains `f813723` plus one side-lane experiment, `1458db9 fix(link): allow timeout settlement grace`, which widened Link/Relay timeout handling. That experiment is intentionally superseded by `958c0a7`, which preserves the 60s wire contract; no required change from `1458db9` remains to be merged into PR #319.

- **Fresh local Rust full test on `958c0a7` — PASS.** `cargo test --workspace` was rerun after all four post-`b10ecff` commits: main unit suite `922 passed; 0 failed`, `service_guardian_process` `1 passed`, and `tcc_broker_process` `4 passed`. `cargo fmt --check` and the full all-target/all-feature Clippy gate were also rerun for the final Rust-bearing candidate.
- **Fresh final Edge regression on `958c0a7` — PASS.** `npm run test:edge` rebuilt root/Edge TypeScript and completed **`388 passed; 0 failed`**. The suite had been `386/386` when the destructive-lifecycle waiver evidence was gathered; the two later timeout/compatibility regressions increased the final-candidate count to 388 without removing any Connector/device/Automation lifecycle coverage.
- **Fresh release-note structural gate on `958c0a7` — PASS.** `node scripts/validate-release-notes.mjs v0.4.6` returned `ok=true`. This remains only the repository structure gate; post-merge user-visible completeness is reviewed separately and is not inferred from the validator.

**B — onboarding**

- **B7 PASS, final candidate live CLI.** With a temporary empty `HOME`, `target/debug/herdr-mcp worker pair` exited 1 with the semantic guidance that the machine has no enrolled device identity, that an existing fleet uses `worker connect`, and that a first device must complete first-Worker Cloudflare bootstrap. No `Io(NotFound)` path was observed.
- **B8 PASS.** The final Rust gate includes `link::daemon::tests::default_placeholder_generation_follows_exact_managed_runtime_current`. On `qingxian-macbookair`, candidate activation then produced an attributable `dev_sync` transition to `rust-3e80b459b26dfc0b` at source `f813723...`; the first subsequent remote state/read/search calls completed without `runtime_generation_superseded_before_dispatch`.
- **B9 PASS, final candidate live runtime.** `link status` on `qingxian-macbookair` reported `operational_ready=true`, current and loaded generation both `rust-3e80b459b26dfc0b`, `loaded_matches_current=true`, `loaded_environment_stale=false`, and `next_action="Operation is ready; no maintainer action required for ordinary use"`.
- **B10 PASS by exact extension compatibility evidence.** Both v0.4.5 and this RC carry extension `0.1.90`; the extension diff from v0.4.5 to the candidate is limited to three locale-line changes and does not change inventory/native-host behavior. `tests/manual/extension_smoke.mjs` and `tests/manual/background_bind_test.mjs` both completed with their `ALL PASS` markers.

**C — lifecycle management**

- **Existing dev authorization recovered without creating a credential.** The shell, launchctl environment, and `.dev.vars` had no `DEV_MCP_BEARER_SECRET`, but the historical Node dev canary Keychain item `herdr-edge-dev-link-secret` already existed. The candidate's legacy-default-workstation compatibility path accepts that existing Link secret for `DEFAULT_WORKSTATION_ID=dev-real-runtime`, so C-group requests reused an already-authorized channel and never printed the secret or placed it on argv.
- **C11/C13 inventory + C14 live output PASS.** The candidate was deployed to the existing isolated `herdr-edge-dev` Worker (`workers_dev=true`, `routes=[]`). Live `/health` reported public contract epoch 3 and runtime contract epoch 2. Authenticated read-only management returned Connector inventory `0` current instances plus `28` legacy clients, device inventory `0`, and Automation inventory `0`. A recursive response-key audit found `0` occurrences of `client_secret`, `client_secret_hash`, `access_token`, `refresh_token`, bearer, verification/approval code, or `device_secret`. This is real live evidence for inventory/auth/rendering, not a unit-test substitution.
- **C11-C13 destructive lifecycle subcases remain test-only under the explicit UAT constraints.** The dev Worker contains no current Connector instance, enrolled device, or Automation Client that is both disposable and already authorized for destructive qualification. Creating one would violate the no-new-credential constraint; revoking one of the 28 historical legacy clients, rotating/revoking a real Automation credential, or revoking a real device would be irreversible and requires the project's explicit confirmation boundary. The `386/386` Edge suite at waiver review covered legacy revoke fencing, immutable-device rename/revoke, Automation rotate/revoke, old-secret invalidation, and issued-token fencing; the final `958c0a7` suite extends that same coverage to `388/388`. These assertions are not mislabeled as live destructive actions.
- **Dev Worker rollback limitation discovered.** Before UAT, `herdr-edge-dev` was deployment `1f98decb-b70b-4152-9f2b-b8373fa619bb`, Worker version `3d9a20cb-8389-4802-8ee7-ea56bf9fb8f6`, and `/health` exposed the old public epoch 1 behavior. Candidate deploy produced version `9a7e5516-1e44-4282-a8f9-8afc78aa5f9e` and advanced Durable Object migration state from v2 to v3. Cloudflare then rejected an exact restore of `3d9a20cb...` with API error `10210`: the old version depends on migration v2 while the current deployment is at v3. The 10-version history contains no earlier v3-compatible version, so repeated old-version rollback attempts would be futile. `herdr-edge-dev` therefore remains on the candidate code; production Edge was not touched.
- **Dev-only provisioning residue.** Wrangler saw the configured `ARTIFACT_BUCKET` binding missing and automatically provisioned `herdr-edge-dev-artifacts` before the attempted interrupt arrived. This created no new Worker or credential, but it is a new Cloudflare R2 resource and exact pre-UAT state is therefore not restored. Deleting/rebuilding Cloudflare resources is destructive and was not attempted without the required explicit confirmation. This residue is a UAT-environment issue and must not be confused with a production deployment.

**D — runtime CPU / EOF recovery**

- **D15-D18 accepted from the existing final-candidate evidence; not rerun.** The retained 76-sample CPU series has mean `0.762%` and peak `6.6%`; while EventCache streams were at EOF, `doctor` remained continuously FAIL with `mode=failed`, and PASS returned only after the replacement server PID was live. The subscriptions recovered after the server returned.
- **`herdr server stop` is a test-method side effect, not a product defect.** Explicit stop sets supervisor `desired_running=false`; the supervisor is correct to honor that operator intent rather than auto-restart. The final Rust gate includes `herdr_supervisor::tests::user_stop_state_is_persistent_and_explicit`. Therefore an explicit stop must not be used to simulate an unexpected crash in future D-group UAT, and no product change is required for this point.

**E — planner-tool integrity**

- **E19 PASS, final candidate live on macOS.** The original live failure was isolated to cross-pane managed-root discovery: a request in `~/.herdr/worktrees/...` could inspect an unrelated TCC-protected Git root and stall inside `projects::broker_git_status`. The fix keeps Herdr-managed linked-worktree validation/search target-scoped while preserving live-snapshot ownership and secret/symlink gates. On `qingxian-macbookair`, `herdr_fs_grep` for `try_grep_rg(` returned `engine=rust`, `3` matches in `6 ms`; a `docs/releases` query for `worker bootstrap` returned `2` matches in `1 ms`. The final Rust gate also covers fail-closed incomplete/timeout behavior, so an unscannable/incomplete search does not become a successful empty result.
- **E20 PASS, live fail-closed validation.** A mutating `pane.split` call using the obsolete/misspelled target parameter `pane` was rejected as `invalid_params / unknown param` before the Herdr socket mutation and created no pane. A second mutating method with an unknown parameter was likewise rejected; no mutation replay was required.
- **E21 inventory live PASS; destructive live reap intentionally not executed.** `instance list` on `yitaidiannao` reports the default instance as installed/running on port 8772 with `reappable=false`, and the historical `uat-v046-ytdn` validation instance as an unloaded `orphan_config` on port 8908 with `reappable=true`. Removing that real config root is irreversible under the project confirmation policy, and no user confirmation phrase was supplied. The final Rust gate covers default-instance non-reapability and named-instance lifecycle isolation; this evidence is not labeled as a live destructive-reap pass.

**Recovery / qualification-lock evidence**

- Routine control-plane failure no longer implies operator restart. Transient Edge errors identify `requires_human=false`; `delivery_uncertain` requires automatic state verification before mutation replay. During this qualification, an uncertain branch-sync/dev-sync window was resolved by `herdr_since`/pane/Git evidence rather than blind replay or user restart.
- The qualification lock was explicitly released only for the intended final `dev sync` and immediately re-armed. `herdr_since` attributed the transition from `rust-e4ceb021704464de` / `3aed5e4...` to `rust-3e80b459b26dfc0b` / `f813723...` with `trigger=dev_sync` and `cursor_reset_reason=runtime_generation_replaced`; the final source is clean and the lock is active.

**Approved qualification waivers (2026-09-06)**

The release owner explicitly approved the following three waivers for the v0.4.6 pre-merge boundary. They close the corresponding qualification blockers without pretending that destructive live actions occurred:

1. **C11-C13 destructive Connector/device/Automation lifecycle — WAIVED.** Live read-only qualification passed against `herdr-edge-dev`: Connector inventory `0` current / `28` legacy, device inventory `0`, Automation inventory `0`, and recursive secret-field audit `0` leaks. Destructive revoke/rotate/fencing semantics were covered by the complete `386/386` Edge suite at waiver review (and remain covered in the final `388/388` suite). Creating a disposable credential solely for qualification would violate the explicit no-new-entity/no-new-credential constraint, while destroying a real historical principal would cross the irreversible-operation boundary. No destructive live credential action is required for merge.
2. **`herdr-edge-dev` rollback to pre-UAT v2 baseline — WAIVED.** Candidate UAT advanced Durable Object migration state from v2 to v3. Cloudflare API error `10210` rejects restoring the old v2-dependent version, and the retained 10-version history contains no v3-compatible pre-candidate baseline. The v3 candidate is therefore accepted as the new **dev-only baseline**; no destructive environment rebuild or rollback is required. Wrangler also automatically provisioned the dev-only R2 bucket `herdr-edge-dev-artifacts`; it remains recorded as qualification residue and is not a production resource.
3. **E21 destructive named-instance reap — WAIVED.** Live inventory proves the default instance is non-reappable and the historical named validation instance is isolated; automatic tests cover reap eligibility/safety. Qualification will not delete a real orphan merely to prove deletion. The known residue **`uat-v046-ytdn`** is explicitly registered for later cleanup: state `orphan_config`, port `8908`, `reappable=true`, pending a future authorized reclaim. It must remain visible in the residue ledger until actually removed.

**Known qualification residue after waiver approval**

- `herdr-edge-dev`: accepted v3 Durable Object migration baseline; exact v2 rollback is impossible through ordinary version restore.
- `herdr-edge-dev-artifacts`: dev-only R2 bucket automatically provisioned by Wrangler during UAT; retained, not production.
- `uat-v046-ytdn`: named UAT instance residue on `yitaidiannao`, `orphan_config`, port `8908`, `reappable=true`; pending later authorized reclaim.

With the three waivers approved, the remaining pre-merge requirement is the normal exact-head GitHub CI on the qualification-record commit. Do not tag, merge `main`, deploy production Edge, or close #302 as part of this pre-merge record.

### 2026-09-06 first-Worker UAT closure and recovery integration

- **Qualification boundary — partial real first-Worker evidence, not a clean-machine E2E pass.** The accurate description of this lane is **"fresh Cloudflare account/fleet + first-Worker candidate bootstrap on an existing production Mac with isolation and compensation"**. The Cloudflare account/fleet was genuinely fresh, but the physical workstation was the existing `yitaidiannao` production Mac, with candidate state isolated behind alternate config roots and explicit compensation/rollback. This must not be cited as a third brand-new Mac E2E. The strict `[7/7] -> operational_ready -> authenticated MCP tool call` sequence did not complete: the final retained attempt stopped at the macOS credential-helper timeout after the Worker/pairing path, then restored the production `0.4.5` runtime. Human intervention was 3 coarse points, or 4 if Cloudflare account creation and API-token creation are counted separately. Because no attempt reached the successful `[7/7]` boundary, there is no reportable successful happy-path duration.
- **PR #319 stale-head merge and recovery integration.** Another lane merged PR #319 at `2026-09-06T08:17:26Z` while its head was still `338b19d5bcd2d8e47321551bf5ff93aa8bbf6dbd`, producing merge commit `36b2a7b5bf04862d4507cea6d8288591d1ecb99a`. The five real first-Worker UAT fixes (`746551d`, `b29a2c7`, `205885a`, `5a42e1f`, `6a81d87`) were rebased and pushed only after that PR had already closed, so those five commits have no PR-level review record. Recovery was deliberately guarded rather than force-updating history: `yitaidiannao` first re-qualified rebased head `6a81d87` with Rust fmt/Clippy/workspace tests plus the full Edge suite; `qingxian-macbookair` then verified a clean worktree and a remote-head guard that `origin/release/v0.4.6-auth-hotfix-20260904` was still exactly `ec5450e` before creating recovery merge `bcc7af8ffec6747e7287fa4f231badfe0dbb8ef9` with parents `ec5450e` and `6a81d87`. The combined release tree was re-qualified a second time (`927` Rust unit tests + `1` guardian + `4` TCC process tests; Edge `388/388`) before push. GitHub CI run #892 (`34022868778`) subsequently completed with both `rust=success` and `test=success`.
- **Non-Git UAT evidence package.** The sanitized evidence remains outside the repository at `~/.herdr/evidence/herdr-mcp/v0.4.6-first-worker-uat-20260906`; its `MANIFEST.sha256` file has SHA-256 `526e945c04a7874f7e96ddad38771acec3fe2e38b098def98e1ed1a49cf8a5d5`. The corresponding archive is `~/.herdr/evidence/herdr-mcp/v0.4.6-first-worker-uat-20260906-sanitized.tar.gz`, SHA-256 `4848508c83521e95aebdee2983cede7609e61583a94e2f877950bc62b5a549ac`. The bundle contains 47 files and its post-sanitization scan recorded `secret_scan=clean`. Keep this evidence in the non-Git evidence store; do not move or copy it into the repository.

### 2026-09-06 exact-`bcc7af8` final regression and cross-interaction review

- **Exact code boundary.** A temporary isolated verification worktree was created at exactly `bcc7af8ffec6747e7287fa4f231badfe0dbb8ef9`; the later `50b21c9` release commit was confirmed documentation-only (`docs/_wip/v0.4.6-release-plan.md`, six added lines). No result from an older code head was reused as the final local regression.
- **Fresh Rust gates — PASS.** `cargo fmt --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, and `cargo clippy --target x86_64-unknown-linux-gnu -p herdr-mcp -- -D warnings` all exited 0. `cargo test --workspace` completed with `927/927` main unit tests, `1/1` service-guardian process test, and `4/4` TCC broker process tests passing.
- **Fresh Edge + release-note structural gates — PASS.** After a fresh `npm ci`, `npm run test:edge` completed `388/388` with zero failures. `node scripts/validate-release-notes.mjs v0.4.6` returned `ok=true` at the exact code boundary; the validator must be rerun again after the final release-note wording because it checks structure, not semantic completeness.
- **Readiness waits do not extend the MCP transport deadline.** The operator-readiness and pairing-pepper-readiness loops each use scheduled delays `0/1/2/4/8s`, for **15 seconds of explicit backoff** per loop. They run inside the local Rust `worker bootstrap` Cloudflare-control path; `19017df` changes the separate Edge MCP `tools/call` transport envelope (tool/local and Link wire remain 60s; Edge settlement may remain alive to 65s). The bootstrap readiness sleeps therefore do not accumulate into the 60/65s MCP deadline or create one combined request-unavailability window. Each bootstrap HTTP attempt remains independently bounded by the existing 20s HTTP timeout, so 15s is the scheduled backoff bound rather than a claim that every adverse-network wall-clock path completes in 15s. Retry scopes also remain narrow: operator readiness accepts only 401/403/5xx, while pairing retries only pre-mutation `503 pairing_unavailable`.
- **Proxy fallback preserves credential redaction.** `5ea545b` still permits proxy fallback only for transport health failures, never contract/ownership validation failures. The exact-head Rust run passed `credentials_are_redacted_from_debug_output_logs_status_and_journal`, `journal_serialization_contains_no_secret_fields`, and `sanitizer_removes_exact_cloudflare_credential`; proxy URLs with userinfo are rejected and SOCKS bootstrap supports no username/password authentication. The proxy path therefore does not add a second credential-storage/logging surface.
- **Qualification lock fences generation replacement before stale-Link repair.** The #314 lock is checked in the service-install/update generation-changing transaction before the post-commit production-Link reconciliation that contains `f3fbd9f`'s stale-loaded-generation reload. With the lock active, generation replacement fails before that reload path becomes reachable; after an allowed generation change, the stale-Link reconcile remains intact. Focused exact-head tests `qualification::tests::held_lock_blocks_generation_change_and_unlock_restores_it`, `link::generation_refresh::tests::active_runtime_control_with_stale_loaded_binary_reloads_link_once`, and `link::generation_refresh::tests::stale_loaded_launchd_generation_reloads_owned_plist` all passed.
- **#302 remains a product decision, not a qualification fact to auto-resolve.** The strict `[7/7] -> operational_ready -> authenticated MCP tool call` path has still not completed once. Release documentation is prepared for the currently observed shipped boundary (major simplification, 3-4 human interventions, possible same-user Keychain/credential-helper block) without marking #302 complete or choosing whether another UAT should delay publication.

### 2026-09-06 post-seal lifecycle/status release blockers (#321 / #322)

- **Release-owner scope correction.** There is no v0.4.7 stabilization line: v0.4.6 is the final 0.x release before 1.0. The release owner explicitly reclassified [#321](https://github.com/whshang/herdr-mcp/issues/321) and [#322](https://github.com/whshang/herdr-mcp/issues/322) from follow-up candidates to **v0.4.6 release blockers**. This is an authorized exception to the earlier code freeze; tag, `main` merge, production Edge deployment, #302 closure, and 1.0 work remain out of scope.
- **#321 live reproduction and root cause.** The release worktree `/Users/qingxian/.herdr/worktrees/herdr-mcp/release-v046-auth-hotfix-20260904` was simultaneously reported by the live Herdr project topology and `worktree.list(workspace_id=w9Y)` as a managed/open linked worktree, while `herdr_mcp.github.status` rejected that exact root with `project_root_not_managed`. The local GitHub-status implementation had a second, narrower ownership predicate that inspected only raw `workspace.worktree.checkout_path` / `workspace.cwd`; it now reuses `projects::derive_routing` plus `fs_security::validate_exact_project_root_with_topology`, the same managed-root truth used by deterministic project tools.
- **#322 real Herdr 0.8.2 boundary proof.** A disposable linked checkout at exact release head `3ff00cc` was created and attached through ordinary `workspace.create`, reproducing the historical-state shape: native `worktree.remove` returned `not_linked_worktree` before any Git removal. Native `worktree.list` nevertheless proved the exact path was `is_linked_worktree=true` with `open_workspace_id` equal to that workspace and identified the parent source workspace. Calling `worktree.open` on the same existing checkout returned `already_open=true`, restored persisted membership, and the following native `worktree.remove` succeeded. The disposable worktree and branch were removed immediately after the proof.
- **#322 compatibility fence.** The v0.4.6 adapter automates only that proven recovery sequence after the first remove returns the exact pre-mutation `not_linked_worktree` error. It derives exactly one managed Git project for the target workspace, requires native `worktree.list` to prove matching path + linked-worktree status + matching open workspace, uses the reported parent source to reopen with `focus=false`, verifies the reopened workspace/path/link identity, and then performs one remove attempt. If any proof is absent or mismatched, the original error is returned unchanged; the path never degrades into a generic shell `git worktree remove` fallback.
- **Focused regressions — PASS.** `github_status::tests::project_root_uses_the_shared_managed_project_topology`, `native_tools::tests::historical_linked_worktree_membership_is_reconciled_before_one_remove_retry`, and `native_tools::tests::worktree_remove_reconcile_stays_fail_closed_without_linked_open_proof` all passed on the blocker-fix branch.
- **Fresh replacement-boundary Rust gates — PASS.** `cargo fmt --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo clippy --target x86_64-unknown-linux-gnu -p herdr-mcp -- -D warnings`, and `cargo test --workspace` all exited 0. The final workspace run contains `929/929` main unit tests, `1/1` service-guardian process test, and `4/4` TCC broker process tests.
- **Fresh Edge regression — PASS after serial flake reconciliation.** The first full Edge run was intentionally started in parallel with the two heavy Rust Clippy lanes and produced exactly one unrelated timing-threshold failure: `revoke wakes a reconnect-grace waiter...` completed in `142ms`; the other `387/388` Edge tests passed and this blocker branch changes no Edge source. After all Rust compilation exited, the affected `device-revoke-kill-switch.test.mjs` file was rerun alone and passed `26/26` (the previously failing wake path completed in `9.2ms`), followed immediately by a serial full `npm run test:edge` at **`388/388`**. This is recorded as load-sensitive test timing, not silently discarded evidence. `node scripts/validate-release-notes.mjs v0.4.6` also returned `ok=true` on the same tree.
- **Independent read-only safety audit — no release blocker.** Droid independently traced the shared managed-root gate, native Herdr error mapping, `worktree.list/open/remove` contracts, and every `call_with_local` path. It found no path that retries removal without the exact initial pre-mutation `not_linked_worktree` error plus positive list/open proof. Residual risks are fail-closed or upstream-identity risks: a missing/mismatched snapshot causes the compatibility path not to fire; an `open` that repairs membership before a later proof failure may leave that metadata repair in place but does not send removal; and the adapter ultimately trusts Herdr's `is_linked_worktree` classification while native Herdr still owns the final removal safety checks. None requires widening this release fix.
- **Implementation PR and exact-head CI — PASS.** [PR #323](https://github.com/whshang/herdr-mcp/pull/323) targeted `release/v0.4.6-auth-hotfix-20260904`. Its first pushed implementation/qualification head was `7b8fc0990c51a6a410c357b75bcbedb70cf4fa62`; the final PR head after adding release-note provenance was `fce44f6be2f0441deb58eedca252b21725e89bb9`. GitHub CI run `34028308245` completed with both `rust=success` and `test=success` on that exact final head. The release-note Main changes entry links #321, #322, and #323 rather than inventing pre-PR provenance.
- **PR #323 merged into release.** After re-fetching and proving final PR head `fce44f6`, base/release head `3ff00cc`, `MERGEABLE`, and exact-head rust/test success, PR #323 was merged at `2026-09-06T10:50:26Z` as merge commit `430ccec0ce9aca0d6a3e888d07ae740deb110534`. The PR body was changed from `Fixes #321/#322` to `Addresses #321/#322` before merge so issue closure remains under release-owner control. #321/#322 therefore remain release tracking items rather than being closed by this integration.
- **Remaining qualification gate.** The replacement code boundary and PR integration are qualified. This qualification-record commit is the final planned release-branch mutation in this lane; after it is pushed, the resulting exact release HEAD must receive the required explicit `workflow_dispatch` CI because ordinary release-branch pushes do not trigger `ci.yml`. No tag, `main` merge, production Edge deployment, #302 closure, or 1.0 work is authorized by this record.
