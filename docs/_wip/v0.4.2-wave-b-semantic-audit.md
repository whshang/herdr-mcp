# v0.4.2 Wave B semantic audit

Date: 2026-08-29

## Historical comparison

No prior number is treated as a machine-comparable baseline unless command, host class, sample count and timing semantics match the new baseline. This prevents manufactured percentage-improvement claims.

Tracked performance/benchmark documents with latency-like prose or data: `3` candidates; retained as context only.

## Dispatch and inventory invariants

- Direct/agent policy references found: `54`.
- Inventory/cache/refresh references found: `129`.
- Skill disclosure/metadata references found: `260`.
- Cleanup/ownership/bounded-lifecycle references found: `645`.

Wave B preserves these invariants:

- deterministic filesystem/Git/search/status work stays on native direct tools;
- capability probing is explicit/bounded and is not inserted into every inspect/tool request; cache misses may use at most four concurrent probe workers while cache/store/final ordering semantics remain deterministic;
- Agent delegation is reserved for reasoning/audit work and is not used as a shell/file-I/O transport;
- Skill payload optimization requires behavior preservation, not payload reduction alone; v0.4.2 keeps Progressive Skills opt-in because default-on would change the frozen epoch-2 `herdr_skill` response semantics without a published per-call legacy escape hatch;
- cleanup never targets dirty/active/unmerged resources without ownership evidence;
- observability stays local/bounded unless analytics is explicitly opted in.
