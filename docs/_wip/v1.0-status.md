# Herdr-MCP 1.0 阶段进度台账

本文件是 1.0 工程阶段的进度 SSOT，只记录当前基线、阶段状态、验收证据与剩余 gate。

- 里程碑序列：[`docs/herdr-architecture-roadmap.md`](../herdr-architecture-roadmap.md)。
- 冻结架构来源：[`docs/history/architecture/v1.0-architecture-plan.md`](../history/architecture/v1.0-architecture-plan.md)，仅作历史 provenance，不回写。
- 发布状态：[`docs/release-model.md`](../release-model.md)。

## 当前统一基线

- 0.4.x maintenance 基线已统一到正式 `v0.4.8`，不再按 v0.4.5/v0.4.6 假设开发。
- Current integration authority: `main`. The former integration branches are historical sources, not release targets.
- Baseline verified on 2026-09-13: source baseline is `origin/main` `8ac614265e76a20268da3c933772d284b43b64d8`, including PRs #364–#416. PR #416 merged after the earlier `f53c71f4` reading, so the integration authority advanced by one merge. Remote main CI reports `8ac61426`; the MacBook Air DEV runtime source remains `f53c71f462091067bd6e072238ac6462d327af6c`. The package version remains `1.0.0-alpha.8`; this is not a GA qualification claim.
- 已继承而不重复实现：Connector/OAuth exact-instance lifecycle/revoke、Automation Client、first-Worker bootstrap、Linux systemd/process/credential/updater、Relay/reconnect/backpressure、Native Messaging/Unix socket、device enrollment/admin、Herdr 0.9 discovery 与 Linux release target。
- 公开合同保持：Workstation Runtime Execution epoch 2 / 18 tools；Public Edge epoch 3 / 19 actions。
- Current StateStore schema is **14** (`state_store::SCHEMA_VERSION`); production v0.4.8 uses schema 5. Real **5 → 14** upgrade, backup restore, and N-1 rollback remain release gates. Older references to schema 8 or 13 describe previous development snapshots.

## alpha.1 — Fleet Control Kernel

- spec：[`v1.0-phase1-fleet-control-kernel.md`](./v1.0-phase1-fleet-control-kernel.md)。
- 已完成：PR #305，merge `0dd89ef`。
- 1.0 继续保留 lease/lane/CAS/fencing 核心；0.4.8 已提供的 Connector/device/operator authority 不再重写。

## alpha.2 — Project Work Memory

- spec：[`v1.0-alpha2-work-memory.md`](./v1.0-alpha2-work-memory.md)。
- 已完成：PR #312，merge `26fd66e`。
- 保留现有 Work Memory / FTS / checkpoint / provider binding；schema 兼容与 rollback 归 rc.1。

## alpha.3 — Browser Endpoint / Resource Registry

- spec：[`v1.0-alpha3-browser-registry.md`](./v1.0-alpha3-browser-registry.md)。
- 已完成：PR #315，merge `6e99bb7`。
- 保留 opaque endpoint/account/space/session refs；对齐 v0.4.8 platform-neutral credentials 与 Herdr machine/device 双命名空间。

## alpha.4 — ChatGPT Web adapter vertical

- spec：[`v1.0-alpha4-chatgpt-adapter.md`](./v1.0-alpha4-chatgpt-adapter.md)。
- 已完成：PR #318，merge `003bf14`。
- Provider/dispatch 逻辑保留；authority glue 以 v0.4.8 exact `connector_id` / grant generation / device scope 为准。

## alpha.5 — Gemini second-provider vertical

- spec：[`v1.0-alpha5-gemini-adapter.md`](./v1.0-alpha5-gemini-adapter.md)。
- 既有实现 HEAD `8dee91a` 已包含于 Alpha 8/v0.4.8 integration 血统。
- 保留 Gemini/Spark adapter、origin consent、reload recovery 与 single-attempt dispatch；剩余真实浏览器 UAT 与统一 browser acceptance 一并执行。

## alpha.6 — Claude provider vertical

- spec：[`v1.0-alpha6-claude-adapter.md`](./v1.0-alpha6-claude-adapter.md)。
- 既有实现 HEAD `91fb8cd` 已包含于统一线。
- 保留 Claude provider/account/session 观察与 provider-neutral dispatch；不新增 Claude 专属状态机。

## alpha.7 — Grok provider vertical

- spec：[`v1.0-alpha7-grok-adapter.md`](./v1.0-alpha7-grok-adapter.md)。
- 已重新汇入 v0.4.8 统一线：`ddd551f` + `8748668`。
- direct `/c/<uuid>` 与 project `/project/<uuid>?chat=<uuid>` 路由均保留；Gemini/Claude/Grok focused regression 13/13 PASS，background bind 与 extension smoke PASS。
- 剩余：真实 signed-in Grok browser UAT。

## alpha.8 — bounded generic Page Assist

- spec：[`v1.0-alpha8-page-assist.md`](./v1.0-alpha8-page-assist.md)。
- 统一基线来源：`8156ada`，之后补入最终 v0.4.8 maintenance delta。
- exact Connector authority、Linux enrolled credential backend 与 Page Assist local origin consent 已共存；公开 contract 未变化。
- 剩余：isolated real-browser UAT；不阻塞后续代码线的本地实现/验证，但最终 acceptance 前必须补齐。

## alpha.9 — Toolchain Efficiency

- spec：[`v1.0-toolchain-efficiency.md`](./v1.0-toolchain-efficiency.md)。
- safe slice：`9ae7320`。
- 已保留 producer-side uniform JSON compaction；约 55% payload savings，raw chunk recovery 保持 lossless，unsafe/marginal rows 保持 raw。
- v0.4.8/main 已提供的 sccache/build-dir/network durable-exec policy 直接继承，不重复开发。
- native-default `herdr_exec` 未在旧 epoch-2 hash 下发布；若未来仍要改变 model-visible VISIBLE-pane 语义，必须独立做 Runtime contract epoch 演进。
- Edge DO request budget：OAuth/Alarm 放大已先行收敛；canonical `device_id` / device-aware ref 的后续路由以 WorkstationDO 持久化 execution fence 作为 fail-closed 执行许可投影，warm path 不再逐次读取 DeviceRegistryDO。DeviceRegistryDO 仍独占 directory/name/alias/desired-state authority；name/alias/legacy-default 继续走 registry。restrictive 状态先 fence Workstation 再写 registry，widening 状态先写 registry 再开放 fence；缺 fence/旧 noncanonical workstation 显式回退 registry，mutation 首次快路径证明 `not_delivered` 后才使用 fresh request id 重试。公开 Edge epoch 与 Runtime epoch 均不变。
- request-budget / 安全回归：warm canonical route = 0 Registry lookup；cold fence = exactly 1 authoritative registry fallback；paused/suspended/revoked pre-send fail-closed；stale fence 不可重开；revoke fence-before-registry；widening partial failure 可重试收敛。最终 `test:edge` 458/458 PASS，0 fail / 0 skip。
- 验收：Rust 992/992 + guardian 1/1 + TCC 4/4，fmt/clippy PASS；JS/Extension focused 47/47 + smoke PASS。

## beta.1 — Browser Reliability / Postcondition

- spec：[`v1.0-beta1-reliability-postcondition.md`](./v1.0-beta1-reliability-postcondition.md)。
- 状态：**local PASS**，统一线 HEAD 已覆盖 reliability boundary。
- 主实现：`3b8c3e5` — durable dispatch identity、late settlement、protocol fail-closed、MV3 target recovery，不重放 mutation。
- 审计修复：`ffa28eb` — durable terminal state 不再被 submit timeout 回退为 `uncertain`；route identity 与当前 conversation 绑定。
- 二次审计修复：`a2de4e1` — monotonic registration-attempt fence，覆盖同路由乱序和 A → B → A 旧响应晚到。
- 交叉审计：上述两类 silent-wrongness 均已关闭，未发现新增 P0/P1/P2。
- 最终本地 gate：Rust 996/996 + guardian 1/1 + TCC 4/4，fmt/clippy PASS；JS/Extension 52/52 + smoke PASS；新增 registration behavior tests 7/7 PASS。
- 边界：`delivery_state=applied` 只表示 prompt submit 已证明，不表示 worker assignment 完成；worker result/lane settlement 属 beta.2。

## beta.2 — WebChat Orchestration / Reservation / Failover

- spec：[`v1.0-beta2-webchat-orchestration.md`](./v1.0-beta2-webchat-orchestration.md)。
- Status: implementation integrated on `main`; real-browser qualification remains open in [#399](https://github.com/whshang/herdr-mcp/issues/399).
- Main includes durable result linkage (#374), lane projection (#375), resource reservation (#377), explicit reassignment (#378), session creation reconciliation (#414), durable current-chat identity (#415), and deferred current-chat self-archive (#416). These merged implementations replace the historical beta.2 development branches.
- Evidence scope: a real signed-in ChatGPT → ChatGPT planner/worker handoff was previously demonstrated and that evidence is not re-derived here. Two concurrent ChatGPT worker lanes with independent settled results, cross-provider (Claude/Grok/Gemini) workers, and the real schema 5 → schema 14 lifecycle have no qualification record in this document.
- 不新增 `WebChatAgent/WebChatTask/Topic` 或第二任务库；直接复用 `work_chain`、`planner_lease`、`execution_lane`、Browser Registry、`browser_dispatch` 与 Work Memory。
- 第一验收链固定为：一个 ChatGPT Planner → 至少两个 ChatGPT worker lanes → independent dispatch → settled assistant result → same Work Chain / Work Memory → Planner 检索聚合。
- Remaining acceptance: prove create/open, independent lane dispatch, durable result retrieval, reservation, explicit reassignment, and account isolation through real signed-in browser sessions. A passed submit or source merge does not prove a settled worker result.
- `uncertain` mutation 禁止自动 failover；device CRUD/pair/revoke/admin 沿用 v0.4.8，不进入 beta.2。
- 旧 `ce25fa1` 的孤立 schema-v9 locator/reservation DDL 不直接移植；只有现有 schema 8 无法安全表达真实 consumer 时才增加最小 migration。

## rc.1 — Security / Migration / Compatibility / Rollback

- Status: implementation and automated checks are in progress; **GA is not qualified**.
- PR #409 provides `update major-apply` / `update major-rollback`, private consistent database snapshots, a preserved executable source binary, integrity checks, and stop-failure recovery. Its unit tests prove these mechanisms; they do not replace a real v0.4.8 → current-schema → v0.4.8 lifecycle qualification. [#398](https://github.com/whshang/herdr-mcp/issues/398) remains open; its schema-13 title is stale and the acceptance target is now schema 14.
- The support matrix and compatibility tests are integrated (#372, #392). macOS and native Linux retain their existing support boundaries. The native Windows implementation is merged on `main` via [#364](https://github.com/whshang/herdr-mcp/pull/364); only the physical Production promotion in [#394](https://github.com/whshang/herdr-mcp/issues/394) is unqualified, so Windows stays Candidate and WSL stays Unsupported. Windows promotion is not implied by this closeout.
- Remaining real-boundary gates: signed-in Gemini/Claude/Grok provider and Page Assist acceptance; two concurrent ChatGPT worker lanes with independent settled results; cross-provider (ChatGPT → Gemini/Claude/Grok) planner handoff and delegation; separate two-device lane/handoff evidence; and the frozen architecture's GitLab plus non-GitLab consumer scenarios. A real signed-in ChatGPT → ChatGPT planner/worker handoff has prior evidence and is not reopened by this list. The two-worker, cross-provider, and v0.4.8 schema 5 → schema 14 upgrade/restore/rollback items have no qualification here.
- Open finding merged with #416: deferred self-archive persists the exact self-archive intent and drains it once the source turn is idle, but `drainPendingSelfArchives()` in `extension/content/wake.js` removes the intent only on `applied`. An archive whose post-click verification returns `uncertain` therefore stays pending and is clicked again on a later drain, while the runtime has already reported `uncertain` to the planner. That is a mutation replay of exactly the kind beta.1 excludes, and it is a real open item owned by a separate active chat — not a reason to treat PR #416 as excluded.
- Release qualification must use the final committed source: authored notes matching Cargo version, full local gate, clean-machine CI, non-publishing Rust Release qualification, artifact/source identity, migration/rollback, and the real client boundary. Keep MacBook Air on DEV and MacBook Pro on v0.4.8 PROD while these gates remain open.

## Mainline convergence audit — 2026-09-13

Observed on 2026-09-13: 13 worktrees, including main. `main` had `dirty=0` at audit start; the seven historical worktrees below carry the preserved uncommitted changes. No historical worktree is an alternative release authority. Preserve dirty/conflicted/recovery work until its exact delta and ownership are resolved; do not merge a stale branch wholesale.

This pass was read-only and bounded: it inspected ancestry (`git rev-list --count`), `git cherry` patch equivalence, dirty/index/conflict state, and original commit subjects. Ancestry and patch equivalence are proven. Content-level successor equivalence for the deep backwards branches below is **not** proven, and full content comparison was stopped deliberately; those refs stay untouched.

| Worktree branch | Observed state | Disposition |
| --- | --- | --- |
| `main` | `dirty=0`; local branch at `f53c71f4`, two commits behind `origin/main` `8ac61426` | Integration authority is `origin/main`; apply release fixes on a branch off `8ac61426` for review. The stale local `main` is not the baseline. |
| `feat/v1.0-beta2-final-20260910` | HEAD contained in main; six changed files, four unmerged paths | Preserve conflict/index state; do not resume the obsolete integration automatically |
| `feat/v1.0-beta2-webchat-orchestration-20260908` | 14 commits outside main ancestry (1 patch-equivalent, 13 unique); ten changed files | Preserve; main already has successor browser/lane implementations. Only the Grok adapter pair has same-subject successors on main (`4c98cbe2`→`ddd551f6`, `0d5ff361`→`8748668e`) |
| `feat/v1.0-completion-20260910` | 20 commits outside main ancestry (2 patch-equivalent, 18 unique); 19 changed files | Preserve; five browser commits have same-subject successors on main (`4167f09f`→`6e9f7842`, `38100050`→`4399bb44`, `5d450514`→`491aacf4`, `26587598`→`243fac9a`, `c32ec1c9`→`d2d16c7b`); review the remaining source deltas independently of old branch history |
| `feat/v1.0-windows-native-20260910` | Clean; 11 commits outside main ancestry: 6 patch-equivalent, 2 unique, 3 merge commits (#344, #353, #356) whose payload commits are patch-equivalent on main | Retain as historical Windows development source. The native Windows implementation is already merged on main via #364; only the physical Production promotion gate #394 is unqualified. The two unique commits have same-subject successors on main (`aaf489ab`→`ee31f182`, `081e183f`→`8e2fcde0`), but subject-level match is weaker evidence than patch equivalence |
| `fix/standalone-visible-path-20260909` | Four changed files | Preserve custom-path work; no release merge without comparison to current installer |
| `fix/v1.0-explicit-chatgpt-recovery-20260910` | Clean; 1 commit outside main ancestry, patch-equivalent under `git cherry` | Absorbed; safe retirement candidate |
| `fix/v1-current-chat-archive-durable-turn-20260913` | Clean; HEAD `45cf63dc` is an ancestor of `origin/main` `8ac61426`; PR #416 merged | Integrated, but retained because a separate active chat still owns this worktree and branch. The merged drain loop still re-actuates an `uncertain` archive attempt instead of failing closed — a real open finding owned by a separate active chat, not a reason to treat the PR as excluded |
| `fix/v1-major-upgrade-qualification-20260913` | Based on `origin/main` `8ac61426`; updater changes in progress | **Active implementation lane owned by the web planner; explicitly retained.** Do not retire it, and do not treat equality with `8ac61426` as redundancy: the branch is where the 1.0 major-upgrade qualification work continues, so its worktree and ref are live |
| `perf/v1.0-exec-wave-20260909` | 4 commits outside main ancestry (1 patch-equivalent, 3 unique); one changed utility execution file | Preserve; frozen VISIBLE-pane contract forbids silently adopting native-default execution |
| `fix/v1-dev-sync-tcc-git-20260912` | HEAD contained in main; two changed files (+322/-3) | Preserve uncommitted broker/source-identity proposal; not required to advance the currently healthy DEV runtime |
| `recovery/pre-refactor-residuals-20260910` | Clean; 12 commits outside main ancestry (1 patch-equivalent, 11 unique) | Preserve recovery source until replacement equivalence is proven; spot checks found partial content overlap with main but no per-commit proof |
| `release/extension-v0.1.91-v048-20260908` | 4 commits outside main ancestry, none patch-equivalent; two changed performance files | Preserve; extension distribution remains independent of runtime publication |

### Retirement candidates with replacement evidence

These refs have varying replacement evidence. Retirement requires exact equivalence, no active owner, and no attached worktree; subject-level matches and preservation snapshots remain unqualified. Retiring proven redundant refs is cleanup, not a release prerequisite.

| Ref | Replacement evidence |
| --- | --- |
| `feat/v1.0-beta2-result-settlement-20260910` (`8ecf0882`) | `git cherry`: 1/1 patch-equivalent to main `b4c68041`; no worktree |
| `fix/v1.0-explicit-chatgpt-recovery-20260910` (`4592bfd5`) | `git cherry`: 1/1 patch-identical to main `8f97d9fe`; worktree clean |
| `fix/v1.0-link-credential-refresh-20260910` (`8de0e95e`) | `git cherry`: 2/2 patch-identical to main `d1bf7382` and `fb82f33a`; no worktree |
| `safety/recovery-cutover-pre-62df152-20260911` (`49a3e2c93c2b420bbbe207ad90456db887b8dfd4`), `safety/recovery-cutover-pre-ea40714-20260911` (`d9d88f4183163da35ec2f84cab72fb26f15ee555`), `safety/remaining-gates-pre-8125803-20260910` (`bf61becd2dfdf3f38a4e84f2d7cde5f91fa6a3aa`), `safety/remaining-gates-pre-rebase-20260910` (`be896df51f51f2d6a090a770a61e61b2017224e5`), `safety/result-provenance-pre-62df152-20260911` (`e9148e7232435a91629514bf06dfe4580e4b8372`), `safety/result-provenance-pre-ea40714-20260911` (`8e2cb5292941189e41a6c606390740873b7884ce`), `safety/uat-beta2-final-pre-ea40714-20260911` (`e4dfcba235d2cb394f62c04cadbc3e30c11bb6d2`) | `git cherry` reports unique=0 for every commit; these are pre-rebase snapshots of work already on main; no worktrees |
| `integ/v1.0-v048-unified-20260908` (`f6a2fecc`) | 10/13 commits patch-equivalent; the 3 unique commits have same-subject successors on main (`3848c353`→`91fb8cdc`, `72e233ca`→`ddd551f6`, `f6a2fecc`→`8748668e`); no worktree. Subject-level match only |
| `preserve/main-dirty-20260911-2358` (`5d15cc47`) | Single preservation commit of a stale main checkout; main has moved 5+ merges past it. Retire only after confirming the preserved diff holds no unreviewed content |

Retained live by ownership (not retirement candidates in any form): `fix/v1-current-chat-archive-durable-turn-20260913` (separate active chat), `fix/v1-release-readiness-20260913` (this lane) and `fix/v1-major-upgrade-qualification-20260913` (web planner's active major-upgrade qualification lane).

Not retirement candidates yet: every worktree listed above with uncommitted changes (`feat/v1.0-beta2-final-20260910`, `feat/v1.0-beta2-webchat-orchestration-20260908`, `feat/v1.0-completion-20260910`, `fix/standalone-visible-path-20260909`, `perf/v1.0-exec-wave-20260909`, `fix/v1-dev-sync-tcc-git-20260912`, `release/extension-v0.1.91-v048-20260908`), plus `feat/v1.0-windows-native-20260910`, `recovery/pre-refactor-residuals-20260910`, `safety/continuity-chatgpt-durable-only-20260912` (preserves the ChatGPT durable-only handoff experiment next to the open #416 archive finding), `safety/link-owner-fallback-pre-main-20260910` (unique commit), and `perf/v1.0-exec-json-compaction-20260908` (one unique commit).

`git cherry` patch matches and same-subject matches are only evidence of textual replacement, not proof that dirty state, dynamic contracts, or rollback obligations can be dropped. No ref, worktree, index, or conflict state was modified by this audit.

Today's critical path is sequential: fix the observed local release-gate failure; qualify main; complete migration/rollback and signed-in acceptance; prepare the exact release commit and artifacts; obtain explicit approval before publishing or merging remotely. Adding new feature branches or broad performance work does not close these gates.

Local verification on 2026-09-13 found that macOS `ps lstart` uses localized date fields. Under the workstation's Chinese locale the process-identity parser could not recognize a confirmed orphan. The two-line fix scopes `LC_ALL=C` to that query and preserves every ownership check. The existing orphan-reaper test failed before the fix and passed afterward; no new test was needed. `scripts/release-gate.sh full` then passed: Rust 1066/1066, guardian 1/1, TCC 4/4, format/lint, Node/Edge tests, extension smoke, isolated Herdr shutdown, shell/package/diff checks. Authored `v1.0.0-alpha.8` notes validate, release-note tests pass 3/3, and the documentation site builds. This is local qualification evidence, not production migration or signed-in UAT acceptance.


## 2026-09-13 local convergence closeout

- Qualified implementation candidate: `c5eb0211084282a0d47d62fd9c20cbc68a803102`, based on remote main `8ac61426`. This combines the release-readiness lane with the major-upgrade fix; both share one integration history. Remote push/PR/merge remains blocked by automatic approval review pending explicit authorization for `whshang/herdr-mcp`. No release tag, publication, or production deployment was performed.
- The updater now confirms the old service has stopped before taking the final snapshot, rejects preexisting rollback material before stopping on a retry, restarts the source service after snapshot failure, and accepts interrupted migration schemas 6–13 for validated rollback. macOS independently verifies explicit launchctl service absence because the v0.4.8 stop implementation can conflate lookup failure with absence.
- Six focused updater tests pass, including final shutdown writes, stale retry rejection, stop failure preservation, source/intermediate-schema rollback, and snapshot-failure restart. Two existing schema-5 tests use the real first five migrations and preserve continuity through schema 14. The lifecycle tests use isolated service stubs; they do not qualify an actual installed v0.4.8-to-1.0 upgrade and N-1 service rollback.
- Full gate at `c5eb0211`: Rust 1067/1067, guardian 1/1, TCC 4/4, Node 584/584, Edge 461/461, format/clippy, documentation build, extension smoke, shell syntax, package dry-run, and diff checks all pass. The isolated Herdr process was stopped and verified absent. Log: `/tmp/herdr-v1-converged-gate-20260913.log` on MacBook Air.
- Native pi handled documentation reconciliation; native cline audited two residual worktrees; native codex independently reviewed the updater and process-locale fixes and found no blocking P1/P2 regression. Completed worker workspaces were closed and verified absent.
- The following local historical refs were retired after a fresh patch-equivalence check against `origin/main`: result-settlement `8ecf08821df28b9abab1c3e3fd30c8ddd85a7a84`, link-credential-refresh `8de0e95e10bbd50b4c613631d19ce4b384b5b193`, and explicit-chatgpt-recovery `4592bfd53c937da6e83db7a6321bde910d5603ab`. The first two had no worktree; the third's clean, unowned worktree was also removed. Earlier inventory tables are the pre-cleanup audit snapshot. No remote refs were deleted.
- Residual TCC source-identity reads in `dev.rs` and `tcc_broker.rs` remain a useful proposal, but the dirty worktree is still attached to workspace `wEJ`; retain it for its owner. Before adoption it needs main-based integration, missing-broker failure validation, and protected-path DEV sync UAT.
- Residual standalone one-step Native Host activation is not on main. Its old status placeholders and visible-path aliases conflict with main's observed browser-load/drift doctor; preserve the source and port only after resolving that behavior. Seven dirty historical worktrees, preservation snapshots, Windows history, and the separately owned self-archive lane are retained.
- GA remains unqualified: issue #398 requires the installed upgrade/backup/N-1 lifecycle; #399 requires remaining signed-in provider, concurrent-worker, cross-provider, and two-device acceptance; #394 requires Windows physical qualification. The merged #416 uncertain self-archive replay finding remains owned by its existing active chat. Previous real ChatGPT-to-ChatGPT handoff evidence remains accepted. A green local gate does not close these acceptance gaps.


## 2026-09-13 installed macOS migration qualification

PR [#417](https://github.com/whshang/herdr-mcp/pull/417) merged as `1a9090f877e672702292aa00166962551a297fc3` after real GitHub Rust, test, Windows and relay checks passed on `ca75fe46`. The corresponding local and remote branch was retired after merge; main and origin/main converged. This supersedes the preceding remote-approval blocker and the blanket missing macOS installed-lifecycle statement.

An independent terminal on MacBook Air ran the published v0.4.8 binary, the verified DEV candidate, and the source rollback in one private named instance (`major-uat-0913`, port 8855):

| Phase | Runtime | Schema | Installed generation | Observation |
| --- | --- | --- | --- | --- |
| Source install | Published 0.4.8 | 5 | `rust-c4674b730b8fd0fd` | Healthy real launchd service; schema created by the old binary |
| Major upgrade | 1.0.0-alpha.8-dev, source `ca75fe46` | 14 | `rust-fb1b697d4a03d1da` | Healthy; representative continuity chain and turn unchanged; private backup and executable hashes valid |
| N-1 rollback | Exact published 0.4.8 | 5 | `rust-c4674b730b8fd0fd` | Healthy; original business rows restored; the deliberately added post-upgrade turn removed |
| Cleanup | Named service uninstalled | — | — | Server/watchdog jobs and all three recorded runtime PIDs absent; port free |

- Source asset SHA-256: `c4674b730b8fd0fde25ffb2b30947673ffecfb012c90d6ab8372376b83de9777`.
- Candidate SHA-256: `fb1b697d4a03d1da6dc5fc9cfcaeb019a0e23e5e8d9f29dc6ac69ca39f5a18fd`.
- The default Air DEV generation `rust-63b10458d47b99b3`, PID 44932, its binary hash, service owner and stable user CLI were unchanged throughout. MacBook Pro was not modified.
- Scope: real macOS installed lifecycle with authentic schema and representative private data. This is not a production fleet migration, signed release-artifact acceptance, or Linux/Windows major-lifecycle qualification. Real browser and release-owner target-client gates remain separate.
- Reproduction and raw assertions were recorded on Air in `/tmp/herdr-major-uat-20260913/lifecycle.py`, `lifecycle.log`, and `result.json`; this table is the durable distilled evidence.

### 2026-09-13 — Browser worker result recovery correction

Two real ChatGPT bootstrap sessions accepted their exact user messages and rendered the requested final answers, while both durable dispatches remained `applied` / `submitted` without result settlement. The pages were unbound to native panes and had automation disabled. Source review confirmed that result reporting had no independent observation trigger for those browser workers.

The correction reuses the existing route timer and dispatch ledger: only proven unfinished assignments are polled; trusted local registration restores the original accepted message and dispatch generation after a page or extension reload. Transient failures back off, repeated explicit identity rejection stops reporting, and an older ACK cannot overwrite a newer assignment. No entity, public tool, or database schema is added. Independent native Codex review found no remaining blocking issue after correcting the initial retry design.

Validation: 33 focused browser tests, 592 Node tests plus background binding smoke, 1,067 Rust tests plus guardian (1) and TCC process tests (4), Clippy and diff checks passed. This is source-level correction evidence. Installed runtime/extension activation and a newly settled two-worker run remain required before claiming the corresponding #399 acceptance scenario passed.

Installed follow-up: Air DEV was activated from clean local main `7e013e11dda80a0a0224633d2eb371551980953a` as generation `rust-c760414ecfa3c576`; service, Link and Native Host agreed, and the PROD 0.4.8 snapshot remained verified. The exact loaded unpacked extension directory was backed up and updated without changing identity. After reload, the test page listener responded but returned null Browser Registry session/generation, so the old dispatch did not settle. Native Host admission was active and site permission was present. The extension files were restored to their prior baseline, reloaded, and the page HUD recovered. This installed browser acceptance is still blocked; the source-level result fix must not be described as an end-to-end pass.

Remote publication of this seven-file fix was rejected by automatic approval review, including after verifying that origin was the same public repository used for #417 and #418. Local main retains the reviewed commit; remote main remains at #418. Explicit authorization for the exact public destination and payload is required before another push attempt.

### 2026-09-13 — Self-archive no-replay correction (extension only)

- Change confined to `extension/content/wake.js` and `tests/browser-actuation-recovery.test.mjs`; commits `99b100a7`, `eb8aada3`, `e708b469`.
- Adds 7 regressions (focused suite 18 → 25); focused + related browser suites 71 PASS; `extension_smoke.mjs` and `background_bind_test.mjs` PASS.
- Grok first blocked 3 issues (post-click window, swallowed storage failure, drain-only serialization), then blocked the menu-await claim race; final targeted re-review is pending main-planner confirmation — not yet accepted.
- The startup-fault root cause is still localized only to an unfinished startup await chain; `7e013e11` is not proven a sufficient cause.
- Air DEV runtime is activated at `7e013e11`; the extension baseline rollback was retained.
- #398 completed; #399 real-browser and #394 Windows acceptance remain open.
- Historical branch review (content/planning): interrupt/placeholder successors are covered, standalone one-step activation is deferred, CLI locale has no 1.0 planning basis — do not merge those branches wholesale.
- Public push remains hostblocked and was not retried; this work is not release-ready.
