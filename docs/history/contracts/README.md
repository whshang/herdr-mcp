# Contract history

Completed contract snapshots live here for audit and migration reference. They are **historical evidence**, not the active runtime contract.

The active public contract is defined by current runtime/code and release metadata. Never update an old snapshot merely to make it match a newer epoch.
