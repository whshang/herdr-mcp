# v0.4.2 production Artifact Relay / R2 UAT — 2026-08-31

Status: **PASS**

This is the final production deploy-upload → Rust import → managed-project read-back evidence required before the `v0.4.2` tag qualification.

## Qualified source and production deploy

- Source commit under test: `f556273d9aff0ba9ab9b62fe7be772e4ec4b8cdd` (`main`, merge of PR #210).
- Runtime source version: `herdr-mcp 0.4.2`, contract epoch `2` / `18` tools, state schema `5`.
- Production Edge workflow: GitHub Actions run `33341703869`, workflow **Cloudflare Edge production**, manually dispatched on `main` at the source commit above.
- Workflow gate: PASS.
- Production R2 provisioning: PASS (`herdr-edge-prod-artifacts`, via the repository `wrangler.prod.toml` contract).
- Production Worker deploy: PASS.
- Workflow workers.dev health verification: PASS.
- Stable custom-domain `/health`: HTTP `200` immediately before the artifact round trip.

The workflow uses the GitHub production Cloudflare secrets; no Cloudflare deployment secret was copied into the UAT evidence.

## Real production round trip

Input fixture: tracked `extension/icons/icon16.png` from the exact source worktree.

1. Upload to `POST https://herdr-mcp.agentforme.cc.cd/artifacts` using the existing production Link bearer read from macOS Keychain only inside the UAT process.
   - HTTP: `201`
   - artifact id: `200b9c5237598d354dc0dc4654d8633c`
   - MIME: `image/png`
   - bytes: `150`
   - SHA-256: `0e306ac74e7faf832f030900fae7b4b6485749e7d8df0fe15a1ee9ce2a0f0d89`
   - the returned object capability was held only in a mode-`0600` temporary file/process variable and is intentionally not recorded here.
2. Build the exact-source release binary with `cargo build --release -p herdr-mcp`: PASS.
3. Import the production relay object with that release binary using `HERDR_ARTIFACT_CAPABILITY` and an absolute path inside the isolated managed final-UAT worktree.
   - result: `ok=true`
   - source: `r2_artifact_relay`
   - MIME: `image/png`
   - bytes: `150`
   - SHA-256: `0e306ac74e7faf832f030900fae7b4b6485749e7d8df0fe15a1ee9ce2a0f0d89`
4. Filesystem read-back: PASS. `shasum -a 256` and byte count exactly matched the upload metadata.
5. MCP visual read-back: PASS. `herdr_fs_image` opened the imported managed-project PNG and reported `image/png`, `150` bytes.
6. Remote cleanup: `DELETE /artifacts/200b9c5237598d354dc0dc4654d8633c` returned HTTP `200`.
7. Post-delete access returned HTTP `404`, proving the temporary relay object was gone.
8. The imported local UAT PNG was removed after read-back; the final-UAT worktree returned clean before this evidence file was added.

## Security / hygiene evidence

- No object capability, Link bearer, Cloudflare token, Authorization header, or browser credential is stored in this document or repository.
- Upload reused the existing production Link principal; object download/delete used only the short-lived object-scoped capability.
- Rust independently verified HTTPS policy, expected SHA-256, MIME/file signature, managed-root admission, and filesystem mutation policy before writing.
- The relay object was explicitly deleted after read-back rather than waiting for TTL expiry.
- The temporary local imported artifact was removed after `herdr_fs_image` verification; only this non-secret evidence document is retained.

## Harness note

An initial version check used `herdr-mcp --version | head -n1`, which caused Rust stdout to report a broken-pipe diagnostic when `head` closed the pipe. This was unrelated to the artifact path and did not affect the successful upload/import/read-back/delete sequence. The exact-source binary was immediately re-verified without truncating its stdout and returned:

```text
herdr-mcp 0.4.2
contract epoch 2 / 18 tools
state schema 5
```

## Gate conclusion

**PASS.** The production Artifact Relay/R2 pre-tag gate is closed for source `f556273d9aff0ba9ab9b62fe7be772e4ec4b8cdd`. After this evidence-only documentation change is merged, the remaining pre-tag blocker is exact-final-source **Rust Release** qualification on the resulting final `main` commit, with `publish` skipped. The immutable `v0.4.2` tag must then point to the exact `release_identity.source_commit` recorded by that successful qualification.
